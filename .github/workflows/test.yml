name: test

on:
  pull_request:
  push:
    paths-ignore:
      - "**/*.md"

jobs:
  test:
    runs-on: macos-14
    steps:
      - uses: actions/checkout@v4
      - name: Run tests
        run: make test
